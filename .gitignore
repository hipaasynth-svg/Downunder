# Secrets / local env
.env

# Agent working state (persisted between runs via the downunder-state branch, not here)
downunder_state.json
daily_brief.txt
nightly_brief.txt
content_*.md
# Cartoon output: rendered strips are ephemeral; the canon cast art lives on the
# downunder-state branch, not in the main repo.
strip_*.png
cast/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
